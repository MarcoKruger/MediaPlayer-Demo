<Page
    x:Class="MediaPlayer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MediaPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Width="700" Height="700">
            <MediaElement Source="Assets/Angels.mp4" Width="400" Height="400" x:Name="Video"></MediaElement>
            <StackPanel Orientation="Horizontal" Width="700" Height="200">
                <Button x:Name="PlayButton" Content="Play" Click="PlayButton_Click" Margin="15"></Button>
                <Button x:Name="StopButton" Content="Stop" Click="StopButton_Click" Margin="15"></Button>
                <Button x:Name="PauseButton" Content="Pause" Click="PauseButton_Click" Margin="15"></Button>
                <Button x:Name="VolumeUp" Content="Add Volume" Click="VolumeUp_Click" Margin="15"></Button>
                <Button x:Name="VolumeDown" Content="Down Volume" Click="VolumeDown_Click" Margin="15"></Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>
